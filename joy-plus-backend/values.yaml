# Default values for joy-plus-backend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: default

replicaCount: 1

image:
  repository: gojoychain/joy-plus-backend
  tag: 0.4.2
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 5555
