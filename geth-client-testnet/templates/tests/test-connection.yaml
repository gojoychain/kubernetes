apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geth-client-testnet.fullname" . }}-test-connection"
  labels:
{{ include "geth-client-testnet.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "geth-client-testnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
